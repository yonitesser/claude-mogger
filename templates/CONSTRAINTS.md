# CONSTRAINTS.md

Every correction you (the user) make to the agents' behavior goes here, not in a
chat message that disappears. The Lead loads this at the start of every
session. Keep entries short — one line, one specific mistake, never repeated.

Format: `- <date>: <what went wrong> → <the rule that prevents it>`

## Corrections

<!-- Example, delete once you have real ones:
- 2026-09-08: builder used a new HTTP client instead of the existing wrapper → always grep for existing usage of a library before adding a new one
-->
